/**
 * Z-index stack for map-adjacent UI inside the console.
 * Must stay below FleetOpsFormDialog (z-99/100) and global chrome (z-120+).
 */
export const MAP_FLOATING_PANEL_Z = "z-10";
export const MAP_CONTEXT_MENU_Z = "z-[95]";
