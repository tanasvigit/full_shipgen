/**
 * @deprecated Use fetchJumpResults from services/jumpToApi.js (live API).
 * Kept for tests that import buildJumpResults — returns empty (no mock fallback).
 */
export const buildJumpResults = () => [];
