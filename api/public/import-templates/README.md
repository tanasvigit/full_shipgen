# Order import templates (on-prem)

Place `Fleetbase_Order_Import_Template.xlsx` here so the console can download it from:

`{API_HOST}/import-templates/Fleetbase_Order_Import_Template.xlsx`

No `fleetbase.io` or `flb-assets` CDN is required.
