# Archived UI documentation

Ember console screen specs were moved here when the product UI moved to `frontend/` (React).

| Archive | Original purpose |
|---------|------------------|
| `ember-console-screens/` | Legacy `console/` routes and templates |

Active UI work should reference:

- `frontend/src/` and `frontend/docs/DEPLOYMENT.md`
- Screen specs under `a_uidocs/screens/fleet-ops/`, `pallet/`, `storefront/`, `ledger/` (API behavior unchanged)
