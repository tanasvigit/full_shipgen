# Service: order-socket-events

**Source:** `packages\fleetops\addon\services\order-socket-events.js`


## Custom actions/tasks (4)

### action `start`

- Guard clause with early return on missing preconditions

### action `_handle`

- Guard clause with early return on missing preconditions

### action `stopAll`

- Guard clause with early return on missing preconditions

### action `_companyHandle`

- Guard clause with early return on missing preconditions

