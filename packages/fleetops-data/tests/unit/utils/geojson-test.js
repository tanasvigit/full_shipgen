import geojson from 'dummy/utils/geojson';
import { module, test } from 'qunit';

module('Unit | Utility | geojson', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = geojson();
        assert.ok(result);
    });
});
