import getSupplierStatusOptions from 'dummy/utils/get-supplier-status-options';
import { module, test } from 'qunit';

module('Unit | Utility | get-supplier-status-options', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getSupplierStatusOptions();
        assert.ok(result);
    });
});
