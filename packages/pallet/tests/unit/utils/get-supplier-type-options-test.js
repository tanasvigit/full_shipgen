import getSupplierTypeOptions from 'dummy/utils/get-supplier-type-options';
import { module, test } from 'qunit';

module('Unit | Utility | get-supplier-type-options', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getSupplierTypeOptions();
        assert.ok(result);
    });
});
