<Layout::Resource::Panel
    @resource={{@model}}
    @controller={{this}}
    @headerTitle="Edit Journal Entry — {{@model.number}}"
    @actionButtons={{this.actionButtons}}
    @saveTask={{this.save}}
    @onPressCancel={{this.cancel}}
    @onOverlayReady={{fn (mut this.overlay)}}
>
    <Journal::Form @resource={{@model}} @controller={{this}} />
    <Spacer @height="200px" />
</Layout::Resource::Panel>