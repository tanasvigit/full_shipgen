<Layout::Resource::Panel
    @resource={{@model}}
    @controller={{this}}
    @headerTitle="Edit Gateway — {{@model.name}}"
    @actionButtons={{this.actionButtons}}
    @saveTask={{this.save}}
    @onPressCancel={{this.cancel}}
    @onOverlayReady={{fn (mut this.overlay)}}
>
    <Gateway::Form @resource={{@model}} @controller={{this}} />
    <Spacer @height="200px" />
</Layout::Resource::Panel>